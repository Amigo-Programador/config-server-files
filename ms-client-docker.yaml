server:
  port: 8081
spring:
  application:
    name: ms-client
  data:
    mongodb:
      database: msclient
      port: '27017'
      host: mongodb-container
  jackson:
    default-property-inclusion: NON_NULL

management:
  endpoint:
    health:
      probes.enabled: true